#include<stdio.h>
void strong(int);
int main()
{
    int lower,upper;
    printf("enter the value of lower limit and upper limit");
    scanf("%d%d",&lower,&upper);
    prime(lower,upper);
    return 0;

}
int strong(int x)
{
    int i,j;
    for(i=x;i<=y;i++)
    {

        {
            if(x%j==0)
                break;
            else if()
    printf("prime number is %d",i);
        }


    }

}


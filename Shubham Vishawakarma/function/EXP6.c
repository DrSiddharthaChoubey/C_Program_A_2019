#include<stdio.h>
void prime(int,int);
int main()
{
    int lower,upper;
    printf("enter the value of lower limit and upper limit");
    scanf("%d%d",&lower,&upper);
    prime(lower,upper);
    return 0;

}
void prime(int x,int y)
{
    int i,j,n,sum=0,a;
    for(i=x;i<=y;i++)
    {


    int fact(n)
        {
            int i,fact=1;
            for(i=1;i<=n;i++)
                f=f*i;
            return (f);
        }
    }
    {n=i;

        whlie(n>0)
        {
            a=n%10;
            sum=sum+fact(a);
            n=n/10;


        }
        if(i==sum)
            printf("%d is a strong number",i);


    }

}
